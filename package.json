{
  "name": "ai-script-agent-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "backend": "npm run dev --workspace=packages/backend",
    "mobile": "npm start --workspace=packages/mobile-app"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "expo-clipboard": "~8.0.7"
  }
}
